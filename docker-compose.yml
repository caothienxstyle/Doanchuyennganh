version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "3000:3000"

  frontend:
    build: ./warehouse-fe
    container_name: frontend
    ports:
      - "5173:5173"

  # sqlserver:
  #   image: mcr.microsoft.com/mssql/server:2022-latest
  #   container_name: sqlserver
  #   environment:
  #     SA_PASSWORD: "Nhatthang@0901"
  #     ACCEPT_EULA: "Y"
  #   ports:
  #     - "1433:1433"